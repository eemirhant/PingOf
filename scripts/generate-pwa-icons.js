/**
 * @deprecated Use scripts/sync-brand-icons.js (npm run pwa:icons).
 * Kept so old docs/commands still sync from the official PingOf logo
 * instead of regenerating the procedural mark.
 */
require("./sync-brand-icons.js");
