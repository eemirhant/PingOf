/** Minimum completed matches required to appear in the ranked leaderboard (PRD KK-15). */
export const MIN_MATCHES_FOR_RANKING = 3;

/** Days after which an unanswered challenge becomes EXPIRED (PRD KK-11). */
export const CHALLENGE_EXPIRY_DAYS = 7;

/** Round-robin league points (PRD KK-12 / Alınan Kararlar). */
export const ROUND_ROBIN_WIN_POINTS = 3;
export const ROUND_ROBIN_LOSS_POINTS = 0;
